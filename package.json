{
  "name": "projects",
  "version": "1.0.0",
  "private": true,
  "workspaces": [le
    "packages/*",
    "clients/**/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  },
  "scripts": {
    "start": "lerna run start",
    "new-version": "lerna version --conventional-commits --yes",
    "diff": "lerna diff"
  }
}
